#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char **argv)
{
  printf("weather data linked list");
  printf("1. get today's data from HERE and store it");
  printf("use a linked list to store the date of the data ");
  printf("search");

  return 0;
}
